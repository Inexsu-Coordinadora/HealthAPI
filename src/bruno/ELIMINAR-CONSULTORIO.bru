meta {
  name: ELIMINAR-CONSULTORIO
  type: http
  seq: 15
}

delete {
  url: http://localhost:3000/api/consultorios/1
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response confirms deletion", function() {
    expect(res.body).to.have.property("mensaje");
    expect(res.body.mensaje).to.include("eliminada");
  });
}
