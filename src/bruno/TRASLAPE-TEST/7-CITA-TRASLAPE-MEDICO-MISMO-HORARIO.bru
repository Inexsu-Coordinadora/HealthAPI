meta {
  name: CITA-TRASLAPE-MEDICO
  type: http
  seq: 7
}

post {
  url: http://localhost:3000/api/citas/agendar
  body: json
}

body:json {
  {
    "idPaciente": 1,
    "idMedico": 1,
    "idDisponibilidad": 1,
    "idConsultorio": 1,
    "fecha": "2025-11-17T09:30:00",
    "motivo": "Seguimiento",
    "observaciones": "El médico ya tiene cita 09:00-10:00"
  }
}

tests {
  test("Status es 400 (traslape detectado)", function() {
    expect(res.getStatus()).to.equal(400);
  });
  
  test("Respuesta contiene mensaje de traslape", function() {
    expect(res.body.error || res.body.mensaje).to.include('traslape');
  });
}
