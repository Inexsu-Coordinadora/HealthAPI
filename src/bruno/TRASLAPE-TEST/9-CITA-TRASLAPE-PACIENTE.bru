meta {
  name: CITA-TRASLAPE-PACIENTE
  type: http
  seq: 9
}

post {
  url: http://localhost:3000/api/citas/agendar
  body: json
}

body:json {
  {
    "idPaciente": 1,
    "idMedico": 1,
    "idDisponibilidad": 1,
    "idConsultorio": 1,
    "fecha": "2025-11-17T09:15:00",
    "motivo": "Intento de traslape en paciente",
    "observaciones": "El paciente ya tiene cita 09:00-10:00"
  }
}

tests {
  test("Status es 400 (traslape de paciente detectado)", function() {
    expect(res.getStatus()).to.equal(400);
  });
  
  test("Respuesta menciona traslape del paciente", function() {
    const error = res.body.error || res.body.mensaje || '';
    expect(error.toLowerCase()).to.include('paciente');
    expect(error.toLowerCase()).to.include('traslape');
  });
}
