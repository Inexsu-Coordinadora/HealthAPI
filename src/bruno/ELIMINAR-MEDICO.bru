meta {
  name: ELIMINAR-MEDICO
  type: http
  seq: 7
}

delete {
  url: http://localhost:3000/api/medicos/1
  body: none
  auth: inherit
}

tests {
  test("Response status should be 200 or 204", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
  
  test("Response should indicate success", function() {
    const status = res.getStatus();
    expect([200, 204]).to.include(status);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
